# ==============================================================================
# 🤖 ENTERPRISE AGENTIC RAG - REQUIREMENTS (WITH VERSIONS)
# Python version used: 3.11.3
# ==============================================================================

# --- CORE API & WEB FRAMEWORK ---
fastapi==0.139.0                    # High-performance web framework for the API
uvicorn[standard]==0.50.2           # ASGI server to run the FastAPI app
python-dotenv==1.2.2                # For loading configuration from .env files
requests==2.34.2                    # HTTP library for internal/external API calls
streamlit==1.59.0                   # Frontend dashboard for the chat interface
numpy==2.4.6                        # Numerical processing
pytz==2026.2                        # Timezone handling for logs
nest-asyncio==1.6.0                 # Support for nested async loops in notebooks/streamlit

# --- GOOGLE CLOUD PLATFORM (GCP) ---
google-cloud-storage==3.12.0        # GCS for Raw and Processed file storage
google-cloud-aiplatform==1.160.0    # Vertex AI (Embeddings)
google-cloud-discoveryengine==0.20.1 # Vertex AI Search services
google-cloud-documentai==3.15.0     # OCR & PDF Parsing via Cloud API (Replaced heavy local parsing)
cloud-sql-python-connector==1.20.4  # Secure connection to Cloud SQL (Postgres)

# --- VECTOR DB & RETRIEVAL ---
qdrant-client==1.18.0               # Vector Database for semantic search
flashrank==0.2.10                   # Ultra-fast local cross-encoder for semantic reranking
redis==8.0.1                        # Memorystore for semantic caching
sqlalchemy==2.0.51                  # ORM for persistent conversation history
pg8000==1.31.5                      # Postgres driver for Cloud SQL

# --- LANGCHAIN & AGENTIC ORCHESTRATION ---
langchain==1.3.11                   # Core LLM orchestration
langgraph==1.2.8                    # State-machine logic for cyclic agent flows
langchain-google-vertexai==3.2.4    # Integration for Google LLMs/Embeddings
langchain-groq==1.1.3                # Integration for lightning-fast Llama 3.3 models
langchain-community==0.4.2          # Community-contributed tools and loaders
langchain-openai==1.3.3             # (Optional) OpenAI compatibility layer
pydantic==2.13.4                    # Data validation and settings management

# --- DATA INGESTION & OFFICE PARSING ---
unstructured==0.24.0                # Base library for document partitioning
python-pptx==1.0.2                  # Parser for PowerPoint files
python-docx==1.2.0                  # Parser for Word documents
pypdf==6.14.2                       # PDF manipulation for splitting large files
beautifulsoup4==4.15.0              # High-speed HTML text extraction

# --- OBSERVABILITY & DISTRIBUTED TRACING ---
langsmith==0.9.8                    # Specialized tracing for LangChain/LangGraph
logfire[fastapi,requests]==4.37.0   # Pydantic Logfire — FastAPI + requests instrumentation
loguru==0.7.3                       # Enhanced logging for Python

# --- GUARDRAILS ---
nemoguardrails==0.23.0              # NVIDIA NeMo Guardrails for input/output safety rails
langchain-nvidia-ai-endpoints==1.4.3 #Langchain integration with NVIDIA AI Endpoints

# --- LLM GATEWAY ---
portkey-ai==2.3.2                   # Portkey Gateway for unified LLM routing, fallbacks & observability

# --- EVALUATION (EVALS) ---
ragas==0.4.3                        # RAG-specific evaluation metrics (Faithfulness, Relevancy, Recall)
deepeval==4.0.7                     # Pytest-compatible test runner wrapping RAGAS & safety metrics
langfuse==4.13.1                    # Live production monitoring, tracing & user feedback collection

sentence-transformers==2.2.2        # Embeddings
